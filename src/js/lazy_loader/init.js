import { viewedProducts } from './viewed_products';
import { relatedProducts } from './related_products';

export const lazyElements = {
  viewedProducts,
  relatedProducts,
};

export default lazyElements;
