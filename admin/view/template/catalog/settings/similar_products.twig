<div class="form-group">
    <label class="col-sm-2 control-label" for="input-titles">Заголовок</label>
    <div class="col-sm-10">
        <input type="text" name="similar_products[title]" value="{{ similar_products.title }}" class="form-control" />
    </div>
</div>

<div class="form-group">
    <label class="col-sm-2 control-label" for="input-limit">Лимит</label>
    <div class="col-sm-10">
        <input type="text" name="similar_products[limit]" value="{{ similar_products.limit }}" id="input-limit" class="form-control" />
    </div>
</div>


<div class="form-group">
    <label class="col-sm-2 control-label" for="input-cnt_diff">Макс. количество отличий чтобы товар появился в списке</label>
    <div class="col-sm-10">
        <input type="text" name="similar_products[diff]" value="{{ similar_products.diff }}" id="input-cnt_diff" class="form-control" />
    </div>
</div>

<div class="form-group">
    <label class="col-sm-2 control-label" for="input-delimiter">Разделитель атрибутов</label>
    <div class="col-sm-10">
        <input type="text" name="similar_products[delimiter]" value="{{ similar_products.delimiter }}" id="input-delimiter" class="form-control" />
    </div>
</div>


<div class="form-group">
    <label class="col-sm-2 control-label" for="input-price_percent">Допустимая разница цен в процентах (скидки и акции не учитываются)</label>
    <div class="col-sm-10">
        <input type="text" name="similar_products[price_percent]" value="{{ similar_products.price_percent }}" id="input-price_percent" class="form-control" />
    </div>
</div>

<div class="form-group">
    <label class="col-sm-2 control-label" for="input-use_excluded_attributes">Следующие отмеченные атрибуты исключить или использовать</label>
    <div class="col-sm-10">
        <select name="similar_products[use_excluded_attributes]" id="input-use_excluded_attributes" class="form-control">
            {%  if similar_products.use_excluded_attributes %}
                <option value="1" selected="selected">Исключать</option>
                <option value="0">Использовать</option>
            {% else %}
                <option value="1">Исключать</option>
                <option value="0" selected="selected">Исполльзовать</option>
            {% endif %}
        </select>
    </div>
</div>


<div class="form-group">
    <label class="col-sm-2 control-label" for="input-excluded_attributes">Атрибуты</label>
    <div class="col-sm-10">
        <div class="well well-sm" style="min-height: 150px;max-height: 500px;overflow: auto;">
            <table class="table table-striped">
                {% for attribute in attributes %}
                    <tr>
                        <td class="checkbox">
                            <label>
                                {% if attribute.attribute_id in similar_products.excluded_attributes %}
                                    <input type="checkbox" name="similar_products[excluded_attributes][]" value="{{ attribute.attribute_id }}" checked="checked" />
                                    {{ attribute.name }}
                                {% else %}
                                    <input type="checkbox" name="similar_products[excluded_attributes][]" value="{{ attribute.attribute_id }}" />
                                    {{ attribute.name }}
                                {% endif %}
                            </label>
                        </td>
                    </tr>
                {% endfor %}
            </table>
        </div>
        <a onclick="$(this).parent().find(':checkbox').prop('checked', true);">{{ text_select_all }}</a> / <a onclick="$(this).parent().find(':checkbox').prop('checked', false);">{{ text_unselect_all }}</a></div>
</div>

<div class="form-group">
    <label class="col-sm-2 control-label" for="input-included_categories"><span data-toggle="tooltip" title="{{ help_included_categories }}">Категория</span></label>
    <div class="col-sm-10">
        <input type="text" name="similar_products[category]" value="" placeholder="Категория" id="input-included_categories" class="form-control" />
        <div id="included_categories" class="well well-sm" style="height: 150px; overflow: auto;">
            {% for category in similar_products.included_categories %}
            <div id="included_categories{{ category.category_id }}"><i class="fa fa-minus-circle"></i>{{ category.name }}
                <input type="hidden" name="similar_products_included_categories[]" value="{{ category.category_id }}" />
            </div>
            {% endfor %}
        </div>
    </div>
</div>


<div class="form-group">
    <label class="col-sm-2 control-label" for="input-status">Статус</label>
    <div class="col-sm-10">
        <select name="similar_products[status]" id="input-status" class="form-control">
            {% if similar_products.status %}
                <option value="1" selected="selected">Включено</option>
                <option value="0">Отключено</option>
            {% else %}
                <option value="1">Включено</option>
                <option value="0" selected="selected">Отключено</option>
            {% endif %}
        </select>
    </div>
</div>

<script>
    // Category
    $('input[name=\'similar_products[category]\']').autocomplete({
        'source': function(request, response) {
            $.ajax({
                url: 'index.php?route=catalog/category/autocomplete&user_token={{ user_token }}&filter_name=' +  encodeURIComponent(request),
                dataType: 'json',
                success: function(json) {
                    response($.map(json, function(item) {
                        return {
                            label: item['name'],
                            value: item['category_id']
                        }
                    }));
                }
            });
        },
        'select': function(item) {
            $('input[name=\'category\']').val('');

            $('#included_categories' + item['value']).remove();

            $('#included_categories').append('<div id="included_categories' + item['value'] + '"><i class="fa fa-minus-circle"></i> ' + item['label'] + '<input type="hidden" name="included_categories[]" value="' + item['value'] + '" /></div>');
        }
    });

    $('#included_categories').delegate('.fa-minus-circle', 'click', function() {
        $(this).parent().remove();
    });

</script>
